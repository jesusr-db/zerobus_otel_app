command: [
  "uvicorn",
  "server.app:app"
]

env:
  - name: CATALOG_NAME
    value: jmr_demo
  - name: SCHEMA_NAME
    value: zerobus_dev

resources:
  - name: warehouse
    sql_warehouse:
      auto_detect: true
      permission: CAN_USE
